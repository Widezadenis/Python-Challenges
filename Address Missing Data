1. Imports: 
Import libraries

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import plotly.express as ex
import seaborn as sns

Load the dataset
df_companies = pd.read_csv("Unicorn_Companies.csv")

2. Data Exploration
Display top rows

df_companies.head(10)

Statistical properties of the dataset: range, data types, mean values, and shape
shape: df_companies.shape
data types: companies.info()
descriptive statistics(mean, standard deviation,range): df_companies.describe()

Data Preprocessing
In what year was the oldest company founded?
df_companies['Year Joined'] = pd.to_datetime(df_companies['Date Joined']).dt.year

Calculate the sum of all valuations of companies from that country
def str_to_num(x):
    x = x.strip('$B')
    x = int(x)

Apply the `str_to_num()` function to the `Valuation` column and assign the result back to a new column called `valuation_num`
df_companies['valuation_num'] = df_companies['Valuation'].apply(str_to_num)
df_companies[['Valuation', 'valuation_num']].head()

Find the number of missing values in each column in this dataset
valuation_num = df_companies.isna().sum()
valuation_num.head()

Review rows with missing values
